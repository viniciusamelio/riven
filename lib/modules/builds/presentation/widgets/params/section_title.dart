import 'package:flutter/material.dart';

class SectionTitleParams {
  final Color? lineColor;
  final Color? textColor;
  final String title;

  SectionTitleParams({this.lineColor, this.textColor, required this.title});
}
